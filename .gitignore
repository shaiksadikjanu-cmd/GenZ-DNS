node_modules/
.vercel/
.env
.env.local
*.log
.DS_Store

# Loose extension ZIPs (only public/GenZ-DNS-Extension.zip is tracked)
/extension.zip
*.zip

# Loose extension ZIPs (only public/GenZ-DNS-Extension.zip is tracked)
/extension.zip
*.zip
